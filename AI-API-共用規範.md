# AI API 共用規範

> 階段一成果。本規範規定全組使用 AI 生成程式碼時的共同基準與機密保管方式。
> 範圍限定在「用哪個模型、key 如何保管、怎麼用」；完整的 Prompt 範本與角色職責屬階段二方法論文件。

---

## 一、模型／工具選定

- **全組統一使用同一個 AI 模型與提供者**。理由：TC-5（模糊 prompt vs 標準範本對照）必須在相同模型下比較，否則差異會混入「模型不同」這個變因，結論不成立。
- 在 `.env` 的 `AI_MODEL` 欄位填入選定的模型名稱，所有人一致。
- 一旦選定，專題期間不任意更換；若必須更換，需記錄更換時間，並重跑受影響的測試案例。

---

## 二、API Key 保管規則（對應 TC-2）

這一節同時是 TC-2「API Key 外洩」要驗證的對象，務必嚴格遵守：

1. **Key 一律寫在 `.env`，從環境變數讀取**，不可寫死（hardcode）在任何 `.py`、設定檔或前端程式中。
2. **`.env` 已被 `.gitignore` 排除**，永遠不進版控。只有 `.env.example`（佔位值）可以進。
3. 程式碼、log、API 回應、錯誤訊息中**不可印出 key**。
4. **不要把 key 貼進 commit 訊息、PR 留言、Issue、聊天截圖**。
5. 一旦懷疑 key 外洩，**立即到提供者後台撤銷並重新產生**，再更新各自的 `.env`。
6. GitLeaks workflow（階段四）會自動掃描，若有人不慎把 key 寫進程式並推上來，PR 會被擋下。

---

## 三、使用流程

1. Prompt Engineer 依階段二的標準 Prompt 範本撰寫需求，呼叫選定的 AI 模型生成程式碼。
2. AI 產出的程式碼**一律經 Reviewer 檢查 + 自動掃描通過後才採用**，不可直接合併。
3. 程式中需要 key 或設定值時，從環境變數讀取（例如 `os.environ["AI_API_KEY"]`），對照 `.env.example` 的欄位名稱。
4. 新增任何機密或設定欄位時，同步更新 `.env.example`（只放佔位值），讓組員知道需要哪些變數。

---

## 四、新成員上手檢查

- [ ] 已 clone repo
- [ ] 已執行 `cp .env.example .env` 並填入真實值
- [ ] 已確認 `.env` 不會被 git 追蹤（`git status` 看不到它）
- [ ] 已與全組確認使用的 `AI_MODEL` 一致
