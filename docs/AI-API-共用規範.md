# AI API 共用規範

> 階段一成果，已對齊「AI 輔助網頁開發技術標準化指引」。
> 範圍：用哪個模型、如何串接、如何確保一致性、key 如何保管。
> 完整 Prompt 範本與角色職責見階段二方法論文件。

---

## 一、模型與存取方式

- **模型**：全組統一使用 **Google Gemini 2.5 Flash**（免費層）。
- **存取方式**：透過 **Gemini API 串接**，不使用聊天網頁介面。
- **為什麼走 API 而非聊天介面**：API 可固定 Temperature 等參數、確保跨批次（不同對話）產出一致，並能接進腳本與自動化。聊天介面隨機性高、難以重現，不利於標準化，也會破壞 TC-5 對照實驗的有效性。
- **為什麼全組統一同一模型**：TC-5 要比較「模糊 prompt vs 標準範本」的產出差異，若各自用不同模型，差異會混入「模型不同」這個變因，結論便不成立。

---

## 二、一致性控管（對應指引第 4 節）

- **Temperature 調低**：固定為 `0.2`（寫在 `.env` 的 `AI_TEMPERATURE`，呼叫 API 時帶入）。程式生成要的是穩定、可重現，不是創意發散。
- **模組化指令**：不要一次叫 AI 生成整個系統；以模組為單位逐一生成，降低記憶偏移與幻覺。
- **Context 重置與重新載入**：當產出品質開始下滑或出現幻覺，立即清除對話、重新載入核心規格，而不是在壞掉的對話上硬改。
- **異常修正流程**：產出不符預期時，先做 Context 重置，再把出錯段落拆更細（邏輯細化）後重新生成。

---

## 三、品質驗證檢查表（對應指引第 4.2 節）

每次採用 AI 產出前，由 Reviewer 對照以下項目：

- [ ] **易讀性**：命名一致、有結構化註解
- [ ] **架構**：符合模組化、無冗餘程式碼
- [ ] **可移植性**：能在指定的 Docker / VM 環境直接運行
- [ ] **規格符合度**：完整覆蓋需求階段定義的邊界條件
- [ ] **安全性**：具備基礎輸入驗證、無明顯漏洞（最終仍以自動掃描為準）

---

## 四、API Key 保管規則（對應 TC-2）

1. Gemini key（新版格式以 `AQ.` 開頭；舊版為 `AIza`）一律寫在 `.env`，從環境變數讀取，**禁止寫死**在程式碼中。
2. `.env` 已被 `.gitignore` 排除，**永不進版控**；只有 `.env.example`（佔位值）可進。
3. 程式碼、log、API 回應、錯誤訊息、commit 訊息、PR 留言、截圖中，**都不可出現 key**。
4. 懷疑外洩時，**立即到 Google AI Studio 撤銷並重新產生**，再更新各自的 `.env`。
5. GitLeaks workflow（階段四）會自動掃描攔截不慎寫進程式的 key。
6. **隱私注意**：免費層的資料可能被 Google 用於改善產品，因此不要把任何機密內容貼進 prompt。

---

## 五、使用流程（Vibe Coding 迴圈）

1. **需求拆解**：定義要做什麼、拆成模組。
2. **編寫標準 Prompt**：套用範本、Temperature 調低。
3. **呼叫 Gemini API 生成程式碼**。
4. **沙盒驗證 ＋ 對照檢查表審查**（在 Docker 內執行）。
5. **通過** → commit、進入安全掃描流程；**未通過** → Context 重置／邏輯細化 → 回到第 2 步。

---

## 六、新成員上手檢查

- [ ] 已 clone repo
- [ ] 已到 Google AI Studio 申請免費 API key
- [ ] 已執行 `cp .env.example .env` 並填入自己的 key
- [ ] 已確認 `.env` 不被 git 追蹤（`git status` 看不到它）
- [ ] 已確認 `AI_MODEL` 與 `AI_TEMPERATURE` 與全組一致
